r56rp_rules = {
	## just something to test various shite on. feel free to use.
	## will not be visible in normal games.

	r56rp_TEST = { 
	
		#cost = 25
	
		icon = GFX_decision_hol_exchange_intelligence_data
	
		visible = { #will not be visible on debug
			is_debug = yes
		}
	
		#target_trigger = {
		#	FROM = {
		#		original_tag = CHI
		#	}
		#}
	
		available = {
			is_ai = no
		}
	
		complete_effect = {
			ROOT.var:r56rp_ht_swap_1 = {
				add_political_power = 50
			}
			ROOT.var:r56rp_ht_swap_2 = {
				add_political_power = 50
			}
			var:r56rp_test_123 = {
				add_political_power = 50
			}
			
		}
	}

	## just something to test various shite on. feel free to use.
	r56rp_TEST_2 = { 
	
		#cost = 25
	
		icon = GFX_decision_hol_exchange_intelligence_data

		visible = { #will not be visible on debug
			is_debug = yes
		}
	
		#target_trigger = {
		#	FROM = {
		#		original_tag = CHI
		#	}
		#}
	
		available = {
			is_ai = no
		}
	
		complete_effect = {

			every_other_country = {
				limit = {
					OR = {
						owns_state = 62
						owns_state = 63
					}
					NOT = { is_in_faction_with = ROOT }
                    NOT = { is_subject_of = ROOT }
				}
				ROOT = {
					create_wargoal = {
						target = PREV
						type = take_state_focus
						generator = { 63 62 }
						expire = 0
					}
				}
			}

		}
	}
}