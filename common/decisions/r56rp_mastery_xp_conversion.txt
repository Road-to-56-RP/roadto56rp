r56rp_mastery_xp_conversion_category = {
	r56rp_convert_doctrine_mastery_increase_infantry = {

		available = {
			has_subdoctrine_in_track = infantry
		}
	
		custom_cost_text = xp_conversion_landxp	
		custom_cost_trigger = {
			has_army_experience > 99	
			has_political_power > 9
	
		}
	
		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				army_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100						# Amount of mastery to add
		
					# FILTERS:
					track = infantry					# Optional - will filter by tracks of the specified type
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_combat_support = {
	
		available = {
			has_subdoctrine_in_track = combat_support
		}
	
		custom_cost_text = xp_conversion_landxp	
		custom_cost_trigger = {
			has_army_experience > 99	
			has_political_power > 9
	
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				army_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100						# Amount of mastery to add
		
					# FILTERS:
					track = combat_support					# Optional - will filter by tracks of the specified type
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_armor = {
	
		available = {
			has_subdoctrine_in_track = armor
		}
	
		custom_cost_text = xp_conversion_landxp	
		custom_cost_trigger = {
			has_army_experience > 99	
			has_political_power > 9
	
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				army_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100						# Amount of mastery to add
		
					# FILTERS:
					track = armor
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_operations = {
	
		available = {
			has_subdoctrine_in_track = operations
		}
	
		custom_cost_text = xp_conversion_landxp	
		custom_cost_trigger = {
			has_army_experience > 99	
			has_political_power > 9
	
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				army_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100						# Amount of mastery to add
		
					# FILTERS:
					track = operations					# Optional - will filter by tracks of the specified type
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_fighter_aircraft = {
	
		available = {
			has_subdoctrine_in_track = fighter_aircraft
		}
	
		custom_cost_text = xp_conversion_airxp	
		custom_cost_trigger = {
			has_air_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				air_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = fighter_aircraft					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_strike_aircraft = {
	
		available = {
			has_subdoctrine_in_track = strike_aircraft
		}
	
		custom_cost_text = xp_conversion_airxp	
		custom_cost_trigger = {
			has_air_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				air_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = strike_aircraft					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_medium_aircraft = {
	
		available = {
			has_subdoctrine_in_track = medium_aircraft
		}
	
		custom_cost_text = xp_conversion_airxp	
		custom_cost_trigger = {
			has_air_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				air_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = medium_aircraft					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_heavy_aircraft = {
	
		available = {
			has_subdoctrine_in_track = heavy_aircraft
		}
	
		custom_cost_text = xp_conversion_airxp	
		custom_cost_trigger = {
			has_air_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				air_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = heavy_aircraft					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_capital_ships = {
	
		available = {
			has_subdoctrine_in_track = capital_ships
		}
	
		custom_cost_text = xp_conversion_navyxp	
		custom_cost_trigger = {
			has_navy_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				navy_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = capital_ships					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_carriers = {
	
		available = {
			has_subdoctrine_in_track = carriers
		}
	
		custom_cost_text = xp_conversion_navyxp	
		custom_cost_trigger = {
			has_navy_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				navy_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = carriers					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_screens = {
	
		available = {
			has_subdoctrine_in_track = screens
		}
	
		custom_cost_text = xp_conversion_navyxp	
		custom_cost_trigger = {
			has_navy_experience > 99
			has_political_power > 9
		}

		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				navy_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = screens					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
	r56rp_convert_doctrine_mastery_increase_submarines = {
	
		available = {
			has_subdoctrine_in_track = submarines
		}
	
		custom_cost_text = xp_conversion_navyxp	
		custom_cost_trigger = {
			has_navy_experience > 99
			has_political_power > 9
		}
		
		days_remove = 90
	
		complete_effect = {
			hidden_effect = {
				navy_experience = -100
				add_political_power = -10
			}
			ROOT = {
				add_mastery = {
					amount = 100									# Amount of mastery to add
		
					# FILTERS:
					# Optional - will filter by tracks of the specified type
					track = submarines					
					#index = 1							# Optional - will filter by the track index within the folder (0-indexed).
				}
			}
		}
	}
}